name: gemini
version: 1.0.0
description: "Google Gemini image generation backend. Uses Gemini 3.1 Flash Image by default and saves generated images to $HERMES_HOME/cache/images/."
author: SymCRG
kind: backend
requires_env:
  - GEMINI_API_KEY
